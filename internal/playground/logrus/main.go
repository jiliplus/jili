package main

import (
	log "github.com/sirupsen/logrus"
)

func main() {
	// log.SetLevel(log.TraceLevel)
	// log.Trace("trace logrus")
	// log.Debug("debug logrus")
	log.Info("info logrus")
	// log.Warn("warn logrus")
	// log.Error("error logrus")
	// log.Fatal("fatal logrus")
	// log.Panic("panic logrus")
}
